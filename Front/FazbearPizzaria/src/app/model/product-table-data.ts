import { _Bottom } from "@angular/cdk/scrolling";

export interface CartData {
  id: number
  nome: string;
  tipo: string;
  preco: number;
  descricao: string;
  quantidade: number;
  imageId: number;
}
